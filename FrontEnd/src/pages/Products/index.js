import React from "react";

const Produts = ({productsArr}) => {
    /*productsArr* Ждать бека, логика : отправка запроса в  зависимости от категории наполнять страницу/ */
    return (
        <div>

        </div>
    )
};

export default Produts